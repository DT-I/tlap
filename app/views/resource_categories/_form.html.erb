<%= form_with(model: resource_category, local: true) do |form| %>
  <% if resource_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(resource_category.errors.count, "error") %> prohibited this resource_category from being saved:</h2>

      <ul>
      <% resource_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :slug %>
    <%= form.text_field :slug %>
  </div>
  


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
